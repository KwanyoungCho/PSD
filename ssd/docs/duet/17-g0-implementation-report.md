# 17 — G0 준비 구현 리포트 (인자 정리 → P0 → E0)

**브랜치**: `feat/duet-p2tree-g0` (base: `feat/mesa-proxy-async-overlap`
@ e29c4b6 = 설계 v6). **목적**: 사용자가 나중에 판단할 수 있도록, 승인된
구현(16번 인자 정리, P0 기록 게이트, E0 수집)의 **모든 이슈·구현 내용·
결과를 이 문서에 기록**한다. 판정 원칙은 설계 v6과 동일 — 자동 문턱
없음, 종합 지표 보고 후 사용자 판단.

**범위** (승인된 것만):
1. **인자 정리** — docs/duet/16 계획의 Tier 1(CLI 별칭) → 2(env 처분)
   → 3(config 단일화). 각 Tier 별도 커밋 + 동일성 검증.
2. **P0** — E0 이중 trace 기록 게이트 (조건: 기본 OFF, TPS 측정 런과
   완전 분리, OFF 오버헤드 0 보증, 독립 모듈로 제거 가능).
3. **E0 수집** — champion 레짐 계측 런 (전용 런, TPS 보고에 사용 안 함).

**엔진 코드 수정 범위 주의**: P2-tree 본체(T1~)는 이 브랜치 범위가
아니다 — 설계 승인 후 별도 진행.

---

## 1. 인자 정리 (16번 계획 실행)

### 1.1 Tier 1 — CLI 별칭

- 상태: 진행 중
- 구현 내용: (기록 예정)
- 이슈: (기록 예정)
- 검증 결과: (기록 예정)

### 1.2 Tier 2 — env 처분

- 상태: 대기

### 1.3 Tier 3 — config 내부 단일화

- 상태: 대기

### 1.4 동일성 검증 (신·구 인터페이스)

- 유닛테스트: (기록 예정)
- champion 스모크 (구 명령 vs 신 명령, PROFILE=0): (기록 예정)
- 기존 테스트 회귀 (M1-M6 38 + jit_subset 5): (기록 예정)

## 2. P0 — E0 기록 게이트

- 상태: 대기 (Tier 1-3 완료 후)

## 3. E0 수집 런

- 상태: 대기

## 이슈 로그 (전체)

| # | 발견 시점 | 내용 | 처리 |
|---|---|---|---|
| (없음) | | | |
