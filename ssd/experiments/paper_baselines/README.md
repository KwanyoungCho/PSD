# Paper Baselines

Paper-facing benchmark outputs are grouped here by experiment family.

## Active Runs

- `ssd_awq_70b_f_sweep_ns32_len512/`: AWQ 70B SSD fan-out sweep, fixed `k=7`, `numseqs=32`, `output_len=512`, `--all`.

## Older / Exploratory

- `ssd_awq_70b/`: earlier AWQ 70B paper-baseline sanity runs with `numseqs=10`, `output_len=256`.
