# Raw Phase-B aligned trace JSONs are large (10s-100s of MB) and
# regenerable from the bundled run scripts. We commit only the
# derived RESULTS / attribution.json / timeline PNGs.
mesa_profile_*.json
mesa_profile_*.json.truncated.bak
